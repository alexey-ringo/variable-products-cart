	<!-- Page info -->
	<div class="page-top-info">
		<div class="container">
			<div class="site-pagination">
				<a href="{{route('shop.index')}}">{{$parent}}</a> /
				{{$title}}
				{{$active}}
			</div>
		</div>
	</div>
	<!-- Page info end -->